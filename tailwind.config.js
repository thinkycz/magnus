module.exports = {
    theme: {
        extend: {
            maxHeight: {
                '40': '8rem'
            },
            minWidth: {
                site: '80rem'
            }
        }
    },
    variants: {
        borderColor: ['responsive', 'hover', 'group-hover'],
        display: ['responsive', 'hover', 'group-hover'],
    },
    plugins: []
};
